# Enable Nginx stub_status for Prometheus scraping
# Add this location block inside your server block in:
# /etc/nginx/sites-available/default

location /stub_status {
    stub_status;
    allow 127.0.0.1;
    deny all;
}
